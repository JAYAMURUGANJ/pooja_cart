import 'package:flutter/material.dart';

class AvailableItemsWidget extends StatelessWidget {
  const AvailableItemsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("data");
  }
}
